.AccountPage
  .AccountPage__title
    %h2 あなたの「素敵」を「もっと素敵」に
  .AccountPage__form
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .FormField
        .FormField__label--normal
          = f.label :email
        .FormField__input--normal
          = f.email_field :email, autofocus: true, value: "aaa@aaa"
        .FormField
          .FormField__label--normal
            = f.label :password
            %i (英数字8文字以上)
          .FormField__input--normal
            = f.password_field :password, autocomplete: "off",  value: "aaaaaaaa"
        .actions
          = f.submit "Log in", class: 'Button UserButton'
  %h5 新規アカウントの作成
  = render "devise/shared/links"
  